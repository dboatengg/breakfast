const Button = () => {
  const style = {
    btn: `py-4 px-8 bg-brand-yellow`,
  };
  return (
    <div>
      <button className={style.btn}>see more</button>
    </div>
  );
};

export default Button;
